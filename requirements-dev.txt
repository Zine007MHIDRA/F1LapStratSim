# Local development + calibration tooling. NOT needed to deploy the app.
#   pip install -r requirements-dev.txt
-r requirements.txt

matplotlib>=3.8      # main.py CLI plots, track_geometry.py __main__
fastf1>=3.4          # calibrate_with_fastf1.py, validate_fastf1.py --fastf1
