# Pre-hydration theme (before theme.inject_css runs) + deploy-friendly server.
[theme]
base = "dark"
primaryColor = "#00F0FF"
backgroundColor = "#0B0E14"
secondaryBackgroundColor = "#141A24"
textColor = "#EEF2F7"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
