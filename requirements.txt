# Runtime dependencies for the Streamlit app (app.py) ONLY.
# The calibration CLIs (calibrate_with_fastf1.py, validate_fastf1.py, main.py)
# need extra packages -- see requirements-dev.txt.
streamlit>=1.50,<2
plotly>=5.24,<8
numpy>=1.26,<3
